export interface BankDTO {
    id?: number
    name: string
    code?: string
    active: boolean
}

export interface BankFormDTO {
    id?: number
    name: string
    code?: string
    active: boolean
}

export interface SelectOptionDTO {
    id: number
    label: string
}
