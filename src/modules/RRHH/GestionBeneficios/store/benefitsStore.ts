// Este archivo ya no es necesario con el patrón correcto de modales
// Los modal IDs se definen en el composable useBenefits.ts
// Los datos se manejan directamente con modalStore.open(MODAL_ID, { data })
export {}
